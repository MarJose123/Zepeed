#!/command/with-contenv sh
# Drop privileges to www-data before exec — s6 starts as root
exec php /var/www/html/artisan queue:work \
    --queue=ping \
    --tries=3 \
    --timeout=120 \
    --sleep=3 \
    --max-jobs=500
